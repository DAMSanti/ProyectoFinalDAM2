version: '3.8'

services:
  proyecto-santi-web:
    build: .
    container_name: proyecto-santi-web
    ports:
      - "8080:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3
